export class Utf8Constants
    {
        public static readonly System = Uint8Array.from(Buffer.from("System"));
        public static readonly SystemReflection = Uint8Array.from(Buffer.from("System.Reflection"));
        public static readonly SystemCollectionsGeneric = Uint8Array.from(Buffer.from("System.Collections.Generic"));
        public static readonly SystemRuntimeInteropServices = Uint8Array.from(Buffer.from("System.Runtime.InteropServices"));
        public static readonly SystemRuntimeCompilerServices = Uint8Array.from(Buffer.from("System.Runtime.CompilerServices"));

        public static readonly Array = Uint8Array.from(Buffer.from("Array"));
        public static readonly Boolean = Uint8Array.from(Buffer.from("Boolean"));
        public static readonly Byte = Uint8Array.from(Buffer.from("Byte"));
        public static readonly Char = Uint8Array.from(Buffer.from("Char"));
        public static readonly Double = Uint8Array.from(Buffer.from("Double"));
        public static readonly Enum = Uint8Array.from(Buffer.from("Enum"));
        public static readonly Int16 = Uint8Array.from(Buffer.from("Int16"));
        public static readonly Int32 = Uint8Array.from(Buffer.from("Int32"));
        public static readonly Int64 = Uint8Array.from(Buffer.from("Int64"));
        public static readonly IntPtr = Uint8Array.from(Buffer.from("IntPtr"));
        public static readonly Object = Uint8Array.from(Buffer.from("Object"));
        public static readonly NullableT = Uint8Array.from(Buffer.from("Nullable`1"));
        public static readonly SByte = Uint8Array.from(Buffer.from("SByte"));
        public static readonly Single = Uint8Array.from(Buffer.from("Single"));
        public static readonly String = Uint8Array.from(Buffer.from("String"));
        public static readonly TypedReference = Uint8Array.from(Buffer.from("TypedReference"));
        public static readonly UInt16 = Uint8Array.from(Buffer.from("UInt16"));
        public static readonly UInt32 = Uint8Array.from(Buffer.from("UInt32"));
        public static readonly UInt64 = Uint8Array.from(Buffer.from("UInt64"));
        public static readonly UIntPtr = Uint8Array.from(Buffer.from("UIntPtr"));
        public static readonly ValueType = Uint8Array.from(Buffer.from("ValueType"));
        public static readonly Void = Uint8Array.from(Buffer.from("Void"));
        public static readonly MulticastDelegate = Uint8Array.from(Buffer.from("MulticastDelegate"));
        public static readonly IEnumerableT = Uint8Array.from(Buffer.from("IEnumerable`1"));
        public static readonly ICollectionT = Uint8Array.from(Buffer.from("ICollection`1"));
        public static readonly IListT = Uint8Array.from(Buffer.from("IList`1"));
        public static readonly IReadOnlyListT = Uint8Array.from(Buffer.from("IReadOnlyList`1"));
        public static readonly Type = Uint8Array.from(Buffer.from("Type"));
        public static readonly DBNull = Uint8Array.from(Buffer.from("DBNull"));
        public static readonly Decimal = Uint8Array.from(Buffer.from("Decimal"));
        public static readonly DateTime = Uint8Array.from(Buffer.from("DateTime"));
        public static readonly ComImportAttribute = Uint8Array.from(Buffer.from("ComImportAttribute"));
        public static readonly DllImportAttribute = Uint8Array.from(Buffer.from("DllImportAttribute"));
        public static readonly CallingConvention = Uint8Array.from(Buffer.from("CallingConvention"));
        public static readonly CharSet = Uint8Array.from(Buffer.from("CharSet"));
        public static readonly MarshalAsAttribute = Uint8Array.from(Buffer.from("MarshalAsAttribute"));
        public static readonly UnmanagedType = Uint8Array.from(Buffer.from("UnmanagedType"));
        public static readonly VarEnum = Uint8Array.from(Buffer.from("VarEnum"));
        public static readonly InAttribute = Uint8Array.from(Buffer.from("InAttribute"));
        public static readonly OutAttriubute = Uint8Array.from(Buffer.from("OutAttribute"));
        public static readonly OptionalAttribute = Uint8Array.from(Buffer.from("OptionalAttribute"));
        public static readonly PreserveSigAttribute = Uint8Array.from(Buffer.from("PreserveSigAttribute"));
        public static readonly FieldOffsetAttribute = Uint8Array.from(Buffer.from("FieldOffsetAttribute"));
        public static readonly IsByRefLikeAttribute = Uint8Array.from(Buffer.from("IsByRefLikeAttribute"));
        public static readonly DecimalConstantAttribute = Uint8Array.from(Buffer.from("DecimalConstantAttribute"));
        public static readonly CustomConstantAttribute = Uint8Array.from(Buffer.from("CustomConstantAttribute"));
        public static readonly GuidAttribute = Uint8Array.from(Buffer.from("GuidAttribute"));
        public static readonly DefaultMemberAttribute = Uint8Array.from(Buffer.from("DefaultMemberAttribute"));
        public static readonly DateTimeConstantAttribute = Uint8Array.from(Buffer.from("DateTimeConstantAttribute"));
        public static readonly CallConvCdecl = Uint8Array.from(Buffer.from("CallConvCdecl"));
        public static readonly CallConvStdcall = Uint8Array.from(Buffer.from("CallConvStdcall"));
        public static readonly CallConvThiscall = Uint8Array.from(Buffer.from("CallConvThiscall"));
        public static readonly CallConvFastcall = Uint8Array.from(Buffer.from("CallConvFastcall"));
    }